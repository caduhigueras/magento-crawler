apiVersion: 1

datasources:
  - name: ClickHouse
    type: vertamedia-clickhouse-datasource
    uid: clickhouse-ds
    access: proxy
    url: http://clickhouse:8123
    basicAuth: true
    basicAuthUser: your_desired_username
    isDefault: true
    editable: true
    jsonData:
      defaultDatabase: crawler
      dataSourceUrl: http://clickhouse:8123
      adHocValuesQuery: "SELECT DISTINCT {field} AS value FROM {database}.{table} LIMIT 300"
    secureJsonData:
      basicAuthPassword: your_desired_password
